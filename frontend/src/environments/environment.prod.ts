export const environment = {
    production: true,
    // En production, les appels API passent par le même hôte (le reverse proxy redirige /api et /ws)
    apiUrl: '',
    appVersion: '1.0.0',
    logLevel: 'error'
  };
